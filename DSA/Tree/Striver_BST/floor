int floor = -1;
while(root){
    if(root->data == key){
        ceil = root->data;
        return ceil;
    }

    if(key<root->data){
        root = root->left;
    }
    else{
        floor = root->data;
        root = root->right;
    }
    return ceil;
}
